/**
 * Você recebe o tempo em horas e precisa retornar o número de litros que vai beber, arredondado para o menor valor.
 *
 * @param time 3
 * @return 1
 */

const litres = (time) => Math.floor(time * 0.5);

export default litres;
